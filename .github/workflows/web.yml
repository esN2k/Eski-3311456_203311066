name: EsenDo-Pages

on:
  push:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2 # Only works with v2
      - uses: subosito/flutter-action@v1
      - uses: esN2k/3311456_203311066@v36.1.1
        with:
          baseHref: /Responsive-ToDo-App-Using-Flutter/